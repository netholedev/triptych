export enum OFFSET_DROP_TIME {
  BEFORE_ARRIVAL = 'Before arrival',
	AFTER_BOOKING = 'After booking',
	AFTER_CONFIRMATION = 'After confirmation',
	AFTER_ARRIVAL = 'After arrival',
	AFTER_DEPARTURE = 'After departure'
}