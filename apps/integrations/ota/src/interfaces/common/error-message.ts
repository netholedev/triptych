export enum ERROR_MESSAGE {
  OTA_VERSION_NOT_SUPPORTED = 'OTA version not supported',
	SESSION_HAS_EXPIRED = 'Session has expired',
	SESSION_ALREADY_CLOSED = 'Session already closed',
	PARAMETER_NOT_SUPPORTED = 'Parameter not supported',
	OTHER = 'Other_'
}