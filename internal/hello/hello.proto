syntax = "proto3";

package hello;

import "google/protobuf/empty.proto";

option go_package = "github.com/netholedev/triptych/internal/hello";

service HelloService {
  rpc SayHi (google.protobuf.Empty) returns (SayHiResponse) {}
}

message SayHiResponse {
	string msg = 1;
}